fish_vi_key_bindings # enable vi mode

set -U fish_greeting # disable fish greeting

# Ctrl-Z suspends a job (like Neovim) and returns to the terminal. Pressing Ctrl-Z again resumes the suspended job. This is an alternative to using a floating terminal in Neovim.
bind -M insert  'fg'
